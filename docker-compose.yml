services:
  bot:
    container_name: 'TarologBot'
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./tarolog:/tarolog
      - ./metrics:/metrics
